<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.incubator3.travniknightrace">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.WebActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.RegisterActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDC6NLwJP5rbnjnBEhO3aBjbxTJC-0Ymrs" />

        <activity android:name=".activities.mapActivity.MapActivity" />
        <activity android:name=".PaymentDetails" />
        <activity android:name=".activities.RaceRegisterActivity" />
        <activity android:name=".activities.propertyActivity.CardActivity"></activity>
        <activity android:name=".activities.propertyActivity.PropertyActivity"></activity>
        <activity android:name=".UserExtraRegistrationActivity"></activity>
        <activity android:name=".activities.OnlineOfflineActivity"></activity>
    </application>

</manifest>